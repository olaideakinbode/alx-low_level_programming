#include "main.h"
void _puts(char *str) /* _puts - prints strings */
#include<stdio.h>
/**
 * main - Entry
 * Return: Always 0 (Success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
