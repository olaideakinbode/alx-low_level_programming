#include "main.h"

/**
 * _strlen - function that returns the length of a string
 * @s: given string
 * Return: 0
 */

int _strlen(char *s)
{
	int counter = 0;

	for (; *s++)
		counter++
		return (counter);

}
